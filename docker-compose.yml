version: "3.8"

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: auth_db
    environment:
      POSTGRES_DB: auth_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: auth_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Commander - Web UI for Redis Management
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: redis_commander
    hostname: redis-commander
    ports:
      - "8081:8081"
    environment:
      - REDIS_HOSTS=local:redis:6379
      - HTTP_USER=admin
      - HTTP_PASSWORD=admin
    depends_on:
      - redis
    restart: unless-stopped

  # Go Auth API Application
  auth-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: auth_api
    ports:
      - "8080:8080"
    environment:
      # Database Configuration
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: root
      DB_NAME: auth_db

      # Redis Configuration
      REDIS_ADDR: redis:6379
      REDIS_DB: 0

      # JWT Configuration
      JWT_SECRET: supersecretjwtkey
      ACCESS_TOKEN_EXPIRATION_MINUTES: 15
      REFRESH_TOKEN_EXPIRATION_HOURS: 720

      # Email Configuration (using your existing settings)
      EMAIL_HOST: mail.horisen.com
      EMAIL_PORT: 587
      EMAIL_USERNAME: goran.jovanovic@horisen.com
      EMAIL_PASSWORD: R2yV_PpDH_Mu5H_MUqA_2GzS
      EMAIL_FROM: goran.jovanovic@horisen.com

      # Social Auth Configuration (using your existing settings)
      GOOGLE_CLIENT_ID: 469439062298-gq7njpnv9c7se122arbvfma02s48448v.apps.googleusercontent.com
      GOOGLE_CLIENT_SECRET: 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
      GOOGLE_REDIRECT_URL: http://localhost:8080/auth/google/callback

      FACEBOOK_CLIENT_ID: 1944774179266724
      FACEBOOK_CLIENT_SECRET: e6b0abb161f008077e6d509a8c9a8bb9
      FACEBOOK_REDIRECT_URL: http://localhost:8080/auth/facebook/callback

      GITHUB_CLIENT_ID: Ov23liESzpSDiICDk8CI
      GITHUB_CLIENT_SECRET: b44d1697d638210251e4980ce450babeb1e05b54
      GITHUB_REDIRECT_URL: http://localhost:8080/auth/github/callback
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
